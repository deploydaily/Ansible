---
- name: AWX install - fetch secrets from Secrets Manager
  ansible.builtin.include_tasks: secrets.yml
  tags: [awx, secrets]

- name: AWX install - preflight checks
  ansible.builtin.include_tasks: preflight.yml
  tags: [awx, preflight]

- name: AWX install - system dependencies
  ansible.builtin.include_tasks: dependencies.yml
  tags: [awx, deps]

- name: AWX install - create user and directories
  ansible.builtin.include_tasks: setup.yml
  tags: [awx, setup]

- name: AWX install - clone and install AWX source
  ansible.builtin.include_tasks: install.yml
  tags: [awx, install]

- name: AWX install - configure AWX settings
  ansible.builtin.include_tasks: configure.yml
  tags: [awx, configure]

- name: AWX install - initialize database
  ansible.builtin.include_tasks: database.yml
  tags: [awx, database]

- name: AWX install - configure systemd services
  ansible.builtin.include_tasks: services.yml
  tags: [awx, services]

- name: AWX install - configure nginx
  ansible.builtin.include_tasks: nginx.yml
  tags: [awx, nginx]

- name: AWX install - health check
  ansible.builtin.include_tasks: healthcheck.yml
  tags: [awx, healthcheck]